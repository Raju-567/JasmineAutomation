//The test suite groups the test cases to be executed. 
//For this manually written file, the test case (it) being added need not be part of the "Test Cases"
describe("<name or title for the spec suite>", function() {
	
	beforeAll(function() {
		//gets executed once before each spec in the describe
	});
	
	afterEach(function() {
		//gets executed once after each spec.
	});
	
	it("<title of the test case/spec>", async function() {
		//write your automation code here
	});
	//more test specs ('it' blocks) can be added
});